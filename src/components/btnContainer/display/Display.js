import React from "react";

export const Display = ({textToDisplay, isOff, isPrank}) => {
    let clsName = "result";
    // const clsName = isOff ? "result off" : "result";
    // const clsName = isPrank ? "prank" : "" 

    if (isOff)
}